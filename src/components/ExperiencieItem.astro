---
interface Props {
  title: string;
  description: string;
  company: string;
  mode: string;
  city: string;
  date: string;
}

const { title, description, company, mode, city, date } = Astro.props;
---

<div
  class="grid grid-cols-1 md:grid-cols-2 relative gap-8 md:gap-[62px] before:content-[''] before:bg-gray-400/80 dark:before:bg-white/60 before:w-px before:h-full before:absolute before:-left-[40px] ml-[50px] pb-20 md:pb-32"
>
  <span
    class="absolute w-4 h-4 bg-gray-700 dark:bg-white block rounded-full -left-12"
  ></span>
  <div class="flex-shrink-0">
    <h3 class="text-xl font-bold">{title}</h3>
    <span class="font-bold opacity-85 block">{city}&nbsp;•&nbsp;{company}</span>
    <span class="text-sm opacity-65 block">{date}</span>
  </div>
  <p class="text-[#888888]">{description}</p>
</div>
